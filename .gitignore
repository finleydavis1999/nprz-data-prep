node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env (.env contains only dev defaults, committed for CI; .env.local for personal overrides)
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
# SQLite
*.db

# Generated data (rebuild with `npm run data`)
# /static/data/ is committed for GitLab Pages deploy; rebuild locally with `npm run data` and commit changes
/raw-data

# Hardcoded users for `npm run users:seed` (commit only seed-users.json.example)
/seed-users.json
